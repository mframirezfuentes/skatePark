<body>
  <h1>Skate Park</h1>

  <div class="py-4">
    <h2>Datos del perfil</h2>
    <hr class="w-50" />
    <form>
      <input class="form-control m-auto" disabled value="{{id}}" id="id" type="hidden" />
      <div class="form-group row w-50 m-auto">
        <div class="form-group col-12 col-sm-6">
          <label>Email</label>
          <input class="form-control m-auto" disabled value="{{email}}" id="email" />
        </div>
        <div class="form-group col-12 col-sm-6">
          <label>Nombre</label>
          <input class="form-control m-auto" value="{{nombre}}" id="nombre" />
        </div>
        <div class="form-group col-12 col-sm-6">
          <label>Password</label>
          <input type="text" class="form-control m-auto" value="{{password}}" id="password" />
        </div>
        <div class="form-group col-12 col-sm-6">
          <label>Repita la password</label>
          <input type="text" class="form-control m-auto" value="{{password}}" id="password1" />
        </div>
        <div class="form-group col-12 col-sm-6">
          <label>Años de experiencia</label>
          <input class="form-control m-auto" value="{{anos_experiencia}}" id="experiencia" type="number" />
        </div>
        <div class="form-group col-12 col-sm-6">
          <label>Especialidad</label>
          <input class="form-control m-auto" value="{{especialidad}}" id="especialidad" />
        </div>
      </div>
      <div class="mb-1">
        <button class="btn btn-primary" id="actualizar">Actualizar</button>
      </div>
      <div>
        <button class="btn btn-danger" id="eliminar">Eliminar cuenta</button>
      </div>
    </form>
  </div>
</body>
<script>
  const eliminar = document.getElementById('eliminar')
  const modificar = document.getElementById('actualizar')

  modificar.addEventListener("click", async (e) => {
    e.preventDefault()
    const id = document.getElementById('id').value
    let nombre = document.getElementById("nombre").value
    let password = document.getElementById("password").value
    let password1 = document.getElementById("password1").value
    let experiencia = document.getElementById("experiencia").value
    let especialidad = document.getElementById("especialidad").value

    let payload = { id, nombre, password1, experiencia, especialidad } 
    const result=  await axios.put("/modificar", payload)
   alert("Usuario actualizado con exito") 
   window.location.href="/"
   
  })
/*
    eliminar.addEventListener("click", async (e, token) => {
      e.preventDefault()
      const id = document.getElementById("id").value
      const payload = id
      console.log("id", id)
      try {
        await axios.delete('/eliminar', payload)
        alert("Skater eliminado de forma exitosa")
  
      } catch ({ response }) {
        const { data } = response
        const { error } = data
        alert(error)
  
      }
  
    })
  */


</script>